// All tunable constants for NeuralPong

export const GAME = {
  WIDTH: 800,
  HEIGHT: 500,
  PADDLE_WIDTH: 12,
  PADDLE_HEIGHT: 80,
  PADDLE_SPEED: 6,
  PADDLE_MARGIN: 20,
  BALL_RADIUS: 6,
  BALL_SPEED: 5,
  BALL_MAX_SPEED: 9,
  BALL_SPEED_INCREMENT: 0.2,
  MAX_SCORE: 5,
  BG_COLOR: '#0a0a1a',
  LINE_COLOR: '#1a1a3a',
  BALL_COLOR: '#00ff88',
  LEFT_PADDLE_COLOR: '#00aaff',
  RIGHT_PADDLE_COLOR: '#ff4488',
  TEXT_COLOR: '#ffffff',
};

export const NEAT = {
  POPULATION_SIZE: 50,
  TARGET_SPECIES: 5,
  INPUTS: 7,
  OUTPUTS: 3,

  // Mutation rates
  WEIGHT_MUTATE_RATE: 0.8,
  WEIGHT_PERTURB_RATE: 0.9,
  WEIGHT_PERTURB_STRENGTH: 0.5,
  WEIGHT_RESET_RANGE: 2.0,
  ADD_CONNECTION_RATE: 0.05,
  ADD_NODE_RATE: 0.03,
  DISABLE_RATE: 0.04,
  ENABLE_RATE: 0.02,

  // Speciation
  EXCESS_COEFF: 1.0,
  DISJOINT_COEFF: 1.0,
  WEIGHT_COEFF: 0.4,
  COMPAT_THRESHOLD: 3.0,
  COMPAT_MODIFIER: 0.3,

  // Reproduction
  ELITISM: 1,
  SURVIVAL_THRESHOLD: 0.2,
  INTERSPECIES_MATE_RATE: 0.001,
  CROSSOVER_RATE: 0.75,
  STAGNATION_LIMIT: 15,
};

export const TRAINING = {
  TICKS_PER_EVAL: 1500,
  TURBO_TICKS_PER_FRAME: 10,
  MAX_TURBO_SPEED: 100,
  AUTO_SAVE_INTERVAL: 30000,
  HEATMAP_COLS: 40,
  HEATMAP_ROWS: 25,
  HEATMAP_DECAY: 0.95,
};

export const FITNESS = {
  SCORE_WEIGHT: 100,
  OPPONENT_SCORE_PENALTY: 50,
  HIT_WEIGHT: 10,
  SURVIVAL_WEIGHT: 0.1,
  IDLE_PENALTY: 0.05,
};
